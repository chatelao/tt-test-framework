# Cool Stuff

**Source:** [https://github.com/PaxPlay/tiny-tapeout-design](https://github.com/PaxPlay/tiny-tapeout-design)

**TinyTapeout Project Page:** [https://app.tinytapeout.com/projects/3558](https://app.tinytapeout.com/projects/3558)

## Input/Output Definitions

| Signal | Type | Width |
|--------|------|-------|
| ui_in | input | 8 |
| uo_out | output | 8 |
| clk | clock | 1 |
| rst_n | input | 1 |

## First 10 Cycles

| Cycle | Phase | ui_in | uo_out | rst_n |
|-------|-------|-------|-------|-------|
| 0 | Reset | 0x0 (MIDI note bit 0=0, MIDI note bit 1=0, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=0) | 0x0 (AudioPWM=0) | 0x0 |
| 1 | A=0, B=0, Cin=0 | 0x0 (MIDI note bit 0=0, MIDI note bit 1=0, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=0) | 0x0 (AudioPWM=0) | 0x1 |
| 2 | A=1, B=0, Cin=0 | 0x1 (MIDI note bit 0=1, MIDI note bit 1=0, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=0) | 0x1 (AudioPWM=0) | 0x1 |
| 3 | A=0, B=1, Cin=0 | 0x2 (MIDI note bit 0=0, MIDI note bit 1=1, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=0) | 0x1 (AudioPWM=0) | 0x1 |
| 4 | A=1, B=1, Cin=0 | 0x3 (MIDI note bit 0=1, MIDI note bit 1=1, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=0) | 0x2 (AudioPWM=0) | 0x1 |
| 5 | A=0, B=0, Cin=1 | 0x80 (MIDI note bit 0=0, MIDI note bit 1=0, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=1) | 0x1 (AudioPWM=0) | 0x1 |
| 6 | A=1, B=0, Cin=1 | 0x81 (MIDI note bit 0=1, MIDI note bit 1=0, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=1) | 0x2 (AudioPWM=0) | 0x1 |
| 7 | A=0, B=1, Cin=1 | 0x82 (MIDI note bit 0=0, MIDI note bit 1=1, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=1) | 0x2 (AudioPWM=0) | 0x1 |
| 8 | A=1, B=1, Cin=1 | 0x83 (MIDI note bit 0=1, MIDI note bit 1=1, MIDI note bit 2=0, MIDI note bit 3=0, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=1) | 0x3 (AudioPWM=0) | 0x1 |
| 9 | Extra logic test 1 | 0xc (MIDI note bit 0=0, MIDI note bit 1=0, MIDI note bit 2=1, MIDI note bit 3=1, MIDI note bit 4=0, MIDI note bit 5=0, MIDI note bit 6=0, Gate (note on)=0) | 0x0 (AudioPWM=0) | 0x1 |

## Bit Patterns

### Input (ui_in)
- **ui_in**: Input signal mappings
![ui_in Input (ui_in)](waveforms/tt3558_ui_in.svg)

### Output (uo_out)
- **uo_out**: Output signal mappings
![uo_out Output (uo_out)](waveforms/tt3558_uo_out.svg)


## Test Waveform

![Waveform](waveforms/tt3558_cool_stuff.svg)
